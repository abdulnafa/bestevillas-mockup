# Synthetic reservation fixtures

Every record in this folder is fictional test data. Names, references, contact details, dates, and amounts are deliberately synthetic and must never be replaced with a real guest export.

Real SiteMinder/Little Hotelier inputs and converted InnBoss files belong outside the repository or under the ignored `.private/reservations/` directory.
